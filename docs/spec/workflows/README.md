# Legacy Workflows (n8n)

This folder contains legacy n8n workflow exports retained for reference. The project has migrated to code-first LangChain pipelines under `docs/spec/pipelines/`.

- Do not modify these JSON files for new work.
- Use the LangChain pipeline designs as the source of truth going forward.
