# Passport Zitadel Implementation

The authentication system now uses the official passport-zitadel package for service account authentication.

Test endpoint: GET /auth/test-passport (requires Bearer token)
