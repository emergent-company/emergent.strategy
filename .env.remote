# Remote Development Environment Configuration
# This file configures the local app to use remote services on dev.emergent-company.ai

# Mode Configuration
NODE_ENV=development
DEPLOYMENT_MODE=remote

# Remote Database Configuration
DATABASE_URL=postgresql://spec:spec@94.130.12.194:5432/zitadel
POSTGRES_HOST=94.130.12.194
POSTGRES_PORT=5432
POSTGRES_USER=spec
POSTGRES_PASSWORD=spec
POSTGRES_DB=zitadel

# Remote Zitadel Configuration
ZITADEL_URL=http://zitadel.dev.emergent-company.ai:8100
ZITADEL_DOMAIN=zitadel.dev.emergent-company.ai
ZITADEL_ORG_ID=347883699234147332
ZITADEL_PROJECT_ID=347883699653577732

# Zitadel Service Accounts (from secrets-dev/service-accounts.json)
# TODO: Replace with actual service account credentials after running setup-zitadel-service-accounts.sh
ZITADEL_CLIENT_SA_KEY_ID=your_client_sa_key_id
ZITADEL_CLIENT_SA_KEY=your_client_sa_key_base64
ZITADEL_API_SA_KEY_ID=your_api_sa_key_id
ZITADEL_API_SA_KEY=your_api_sa_key_base64

# Local Application Ports
SERVER_PORT=3001
ADMIN_PORT=5175

# Skip Local Dependencies (using remote)
SKIP_DOCKER_DEPS=true

# API Configuration
API_BASE_URL=http://localhost:3001

# Encryption Keys (same as production)
INTEGRATION_ENCRYPTION_KEY=your_integration_encryption_key_here

# Feature Flags
SCOPES_DISABLED=0
