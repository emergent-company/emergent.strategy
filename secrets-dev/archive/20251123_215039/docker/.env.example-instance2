# Docker Compose Environment Variables - Instance 2
# This is an example configuration for running a second instance

# Service prefix - CHANGE THIS for each instance
COMPOSE_PROJECT_NAME=spec2

# Database Configuration
POSTGRES_USER=spec
POSTGRES_PASSWORD=spec
POSTGRES_DB=spec
POSTGRES_PORT=5433

# Zitadel Ports - CHANGE THESE to avoid conflicts
ZITADEL_HTTP_PORT=8200
ZITADEL_LOGIN_PORT=8201
