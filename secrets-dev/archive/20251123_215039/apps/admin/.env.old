# OIDC issuer (Zitadel in docker)
VITE_ZITADEL_ISSUER=http://localhost:8200

# SPA Public client (PKCE) - OAuth client ID from Zitadel bootstrap
VITE_ZITADEL_CLIENT_ID=347255068191424515
VITE_ZITADEL_REDIRECT_URI=http://localhost:5176/auth/callback
VITE_ZITADEL_POST_LOGOUT_REDIRECT_URI=http://localhost:5176/
VITE_ZITADEL_SCOPES=openid profile email

VITE_AUTH_MODE=credentials

# Admin dev server port (this instance uses 5176 to avoid conflict with other instance on 5175)
ADMIN_PORT=5176

# Backend API server port
SERVER_PORT=3002
