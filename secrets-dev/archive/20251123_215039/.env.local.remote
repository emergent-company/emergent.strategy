# =============================================================================
# REMOTE DEVELOPMENT ENVIRONMENT CONFIGURATION
# =============================================================================
#
# This file configures the local application to connect to REMOTE services
# running on dev.emergent-company.ai (94.130.12.194)
#
# USAGE:
#   1. Copy this file to .env.local:
#      cp .env.local.remote .env.local
#
#   2. Also copy apps/server/.env.local.remote to apps/server/.env.local:
#      cp apps/server/.env.local.remote apps/server/.env.local
#
#   3. Start services (will skip dependencies):
#      nx run workspace-cli:workspace:start -- --skip-deps
#
# =============================================================================

# Workspace Configuration (overrides .env)
NAMESPACE=spec-server-2-remote

# Service Ports (keep local ports)
ADMIN_PORT=5176
SERVER_PORT=3002

# Remote Zitadel Configuration
ZITADEL_DOMAIN=zitadel.dev.emergent-company.ai

# Skip local dependencies (use remote instead)
SKIP_DEPENDENCIES=true

# Test Users (same as bootstrap)
E2E_TEST_USER_EMAIL=e2e-test@example.com
E2E_TEST_USER_PASSWORD=E2eTestPassword123!
TEST_USER_EMAIL=test@example.com
TEST_USER_PASSWORD=TestPassword123!

# External Tools Configuration (point to remote)
POSTGRES_HOST=94.130.12.194
POSTGRES_PORT=5432
POSTGRES_USER=spec
POSTGRES_DB=zitadel
# Add POSTGRES_PASSWORD in .env.local if needed for MCP tools
