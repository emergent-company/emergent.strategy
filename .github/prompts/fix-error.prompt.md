---
description: Fix recent error
agent: build
---

Check the all logs by executing:

```bash
npm run workspace:logs -- --lines 200
```

Look for error, if needed use playwright mcp to open the browser.

Here is a problem you should be focused on:

<UserRequest>
  $ARGUMENTS
</UserRequest>