# (Removed) Passkey / WebAuthn Authentication

This specification has been deprecated and the underlying implementation removed. The system now relies exclusively on standard OIDC (Zitadel-hosted) authentication flows.

Historical content intentionally deleted to avoid confusion.
