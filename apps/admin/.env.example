# Vite/Nexus Admin UI — Zitadel (Dev)
# Copy to .env.local (Vite will load it) and adjust as needed.

# OIDC issuer (from local docker Zitadel)
VITE_ZITADEL_ISSUER=http://localhost:8080

# Public client (PKCE). Create in Zitadel > Your Project > Applications
VITE_ZITADEL_CLIENT_ID=335516384895238147
# 335516384895238147

# SPA redirects (match your Vite dev server port shown on startup)
VITE_ZITADEL_REDIRECT_URI=http://localhost:5175/auth/callback
VITE_ZITADEL_POST_LOGOUT_REDIRECT_URI=http://localhost:5175/
# Dev server port (single canonical env name). Update if you change it.
ADMIN_PORT=5175

# Scopes and audience
VITE_ZITADEL_SCOPES=openid profile email offline_access
VITE_ZITADEL_AUDIENCE=spec-api

// API base URL (your backend). If omitted, UI will call same-origin /api (via Vite proxy in dev)
# Backend API URL (leave empty to use Vite proxy)
# VITE_API_BASE=http://localhost:3001

# Optional environment flag
VITE_ENV=development
