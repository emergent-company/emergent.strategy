# Admin (Vite) local env

# OIDC issuer (Zitadel in docker)
VITE_ZITADEL_ISSUER=http://localhost:8200

# SPA Public client (PKCE) — OAuth OIDC Application from bootstrap
VITE_ZITADEL_CLIENT_ID=345528629968568323

# Redirects (must exactly match the values configured in Zitadel for this app)
# Adjust port if Vite uses a different one; default is 5176
VITE_ZITADEL_REDIRECT_URI=http://localhost:5176/auth/callback
VITE_ZITADEL_POST_LOGOUT_REDIRECT_URI=http://localhost:5176/
# Dev server port (single canonical env variable)
ADMIN_PORT=5176

# Scopes (offline_access optional if you plan to use refresh tokens later)
VITE_ZITADEL_SCOPES=openid profile email

# Optional: audience for your API (only set if configured in Zitadel)
# VITE_ZITADEL_AUDIENCE=spec-api

# Backend API base URL (leave empty to use Vite proxy)
# VITE_API_BASE=http://localhost:3001

# Test user credentials from bootstrap script
E2E_AUTH_TOKEN=""
E2E_REAL_LOGIN=1
E2E_OIDC_EMAIL="test@example.com"
E2E_OIDC_PASSWORD="TestPassword123!"
# Point E2E tests to the admin dev server (defaults to ADMIN_PORT or 5176)
# Override if you run on a different port
E2E_BASE_URL=http://localhost:5176
E2E_OIDC_BAD_PASSWORD="___definitely_wrong__"
