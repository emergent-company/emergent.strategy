# E2E Test Configuration Example
# Copy to .env.e2e and customize for your environment
# This file is loaded by playwright.config.ts for E2E test runs

# E2E Test User Credentials
# These should match the values in the root .env file (created by bootstrap script)
# The bootstrap script creates this user automatically at e2e-test@example.com
E2E_TEST_USER_EMAIL=e2e-test@example.com
E2E_TEST_USER_PASSWORD=E2eTestPassword123!

# Optional: Auth token for API tests (leave empty for UI-based authentication)
E2E_AUTH_TOKEN=

# Application Ports
# Admin port (default 5175, but PM2 may use 5176)
ADMIN_PORT=5176

# Backend server port (must match the running server port)
SERVER_PORT=3002

# E2E Base URL
# Commented out to let Playwright auto-start the dev server
# Uncomment if you want to test against a specific URL
# E2E_BASE_URL=http://localhost:5176

# Test Data
# Bad password for testing authentication failures
E2E_OIDC_BAD_PASSWORD=___definitely_wrong__
