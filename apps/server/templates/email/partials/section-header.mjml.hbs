{{!--
  Section header partial
  Usage: {{> section-header title="New Features" icon="🚀"}}
  
  Parameters:
  - title: Section title text (required)
  - icon: Optional emoji icon to display before title
  - color: Optional header color (defaults to #374151)
--}}
<mj-text font-size="16px" font-weight="600" color="{{#if color}}{{color}}{{else}}#374151{{/if}}" padding-top="24px" padding-bottom="12px">
  {{#if icon}}{{icon}} {{/if}}{{title}}
</mj-text>
