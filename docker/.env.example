# ===========================================
# ENVIRONMENT CONFIGURATION
# ===========================================
# Environment type - determines which Infisical environment to use (REQUIRED)
# Options: local | dev | staging | production
ENVIRONMENT=dev

# ===========================================
# INFISICAL CONFIGURATION (REQUIRED FOR COOLIFY)
# ===========================================
# Set these in Coolify environment variables
# Get your token from: https://infiscal.kucharz.net

# Service Token for fetching secrets
INFISICAL_TOKEN=st.your-token-here

# Environment to load (dev, staging, production)
# Note: This is set automatically from ENVIRONMENT above
# Can be overridden if needed
INFISICAL_ENVIRONMENT=${ENVIRONMENT}

# Project ID (fixed for this project)
INFISICAL_PROJECT_ID=2c273128-5d01-4156-a134-be9511d99c61

# ===========================================
# LOCAL FALLBACK VALUES (if Infisical unavailable)
# ===========================================
# These are used when running without Infisical or as defaults
# In production, these should come from Infisical

# Database Configuration
POSTGRES_USER=spec
POSTGRES_PASSWORD=spec
POSTGRES_DB=spec
POSTGRES_PORT=5432

# Zitadel Database Configuration
ZITADEL_DB_PASSWORD=zitadel

# Database Logs Configuration
# Options:
#   - Coolify deployment (default): /home/emergent/db_logs
#   - Local dev (relative path): ./logs
#   - Named volume: db_logs
#   - Custom absolute path: /path/to/logs
DB_LOGS_PATH=/home/emergent/db_logs

# Postgres Logging Level
# Options: none, ddl, mod, all
# - none: No statement logging (production default)
# - ddl: Log DDL statements only
# - mod: Log all modifying statements
# - all: Log all statements (verbose, dev only)
POSTGRES_LOG_STATEMENT=none

# Zitadel HTTP Port (API)
ZITADEL_HTTP_PORT=8100

# Zitadel Login v2 Port
ZITADEL_LOGIN_PORT=8101
