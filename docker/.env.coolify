# ===========================================
# COOLIFY DEPLOYMENT CONFIGURATION
# ===========================================
# These variables are used by docker-compose.yml for variable substitution
# Set these in Coolify's environment variable configuration
#
# NOTE: Actual secret values (passwords, tokens, etc.) come from Infisical
# These are only for docker-compose.yml placeholders and port mappings

# ===========================================
# INFISICAL CONFIGURATION (REQUIRED)
# ===========================================
INFISICAL_TOKEN=${INFISICAL_TOKEN}
INFISICAL_ENVIRONMENT=dev
INFISICAL_PROJECT_ID=2c273128-5d01-4156-a134-be9511d99c61
INFISICAL_API_URL=https://infiscal.kucharz.net

# ===========================================
# DOCKER COMPOSE PLACEHOLDERS
# ===========================================
# These are used ONLY for docker-compose.yml variable substitution
# Actual values will be overridden by Infisical at runtime

# Database - used for healthcheck placeholder only
POSTGRES_USER=spec
POSTGRES_DB=spec
POSTGRES_PORT=5432
POSTGRES_LOG_STATEMENT=none

# Database logs path (Coolify server)
DB_LOGS_PATH=/home/emergent/db_logs

# Zitadel ports
ZITADEL_HTTP_PORT=8100
ZITADEL_LOGIN_PORT=8101

# Zitadel external domain (used in Traefik labels)
ZITADEL_EXTERNALDOMAIN=zitadel.yourdomain.com
