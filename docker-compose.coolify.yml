# Docker Compose for Coolify Deployment
# This file explicitly sets build contexts to help Coolify
# understand the repository structure

version: "3.8"

services:
  # PostgreSQL Database with pgvector extension
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-spec}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB:-spec}
      ZITADEL_DB_PASSWORD: ${ZITADEL_DB_PASSWORD:-zitadel}
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./docker/init.sql:/docker-entrypoint-initdb.d/00-init.sql:ro
      - ./docker/01-init-zitadel.sh:/docker-entrypoint-initdb.d/01-init-zitadel.sh:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U spec -d spec && pg_isready -U zitadel -d zitadel"]
      interval: 5s
      timeout: 3s
      retries: 15
      start_period: 15s

  # Zitadel Identity Provider
  zitadel:
    image: ghcr.io/zitadel/zitadel:latest
    depends_on:
      db:
        condition: service_healthy
    environment:
      # Master key (must be 32 characters)
      ZITADEL_MASTERKEY: ${ZITADEL_MASTERKEY}

      # External access
      ZITADEL_EXTERNALDOMAIN: ${ZITADEL_EXTERNALDOMAIN}
      ZITADEL_EXTERNALSECURE: ${ZITADEL_EXTERNALSECURE:-true}
      ZITADEL_TLS_ENABLED: ${ZITADEL_TLS_ENABLED:-false}

      # Database connection
      ZITADEL_DATABASE_POSTGRES_HOST: db
      ZITADEL_DATABASE_POSTGRES_PORT: 5432
      ZITADEL_DATABASE_POSTGRES_DATABASE: zitadel
      ZITADEL_DATABASE_POSTGRES_USER_USERNAME: zitadel
      ZITADEL_DATABASE_POSTGRES_USER_PASSWORD: ${ZITADEL_DB_PASSWORD}
      ZITADEL_DATABASE_POSTGRES_USER_SSL_MODE: disable

      # Admin connection (for initialization)
      ZITADEL_DATABASE_POSTGRES_ADMIN_USERNAME: ${POSTGRES_USER:-spec}
      ZITADEL_DATABASE_POSTGRES_ADMIN_PASSWORD: ${POSTGRES_PASSWORD}
      ZITADEL_DATABASE_POSTGRES_ADMIN_SSL_MODE: disable
      ZITADEL_DATABASE_POSTGRES_ADMIN_HOST: db
      ZITADEL_DATABASE_POSTGRES_ADMIN_PORT: 5432

      # First instance setup
      ZITADEL_FIRSTINSTANCE_ORG_NAME: ${ZITADEL_ORG_NAME:-Spec, Inc.}
      ZITADEL_FIRSTINSTANCE_ORG_HUMAN_USERNAME: ${ZITADEL_ADMIN_USERNAME:-admin}
      ZITADEL_FIRSTINSTANCE_ORG_HUMAN_PASSWORD: ${ZITADEL_ADMIN_PASSWORD}

      # Login v2 configuration
      ZITADEL_DEFAULTINSTANCE_FEATURES_LOGINV2_REQUIRED: "true"
      ZITADEL_DEFAULTINSTANCE_FEATURES_LOGINV2_BASEURI: ${ZITADEL_LOGIN_URL}/ui/v2/login
      ZITADEL_OIDC_DEFAULTLOGINURLV2: ${ZITADEL_LOGIN_URL}/ui/v2/login/login?authRequest=
      ZITADEL_OIDC_DEFAULTLOGOUTURLV2: ${ZITADEL_LOGIN_URL}/ui/v2/login/logout?post_logout_redirect=
    command: ["start-from-init", "--masterkeyFromEnv"]
    expose:
      - "8080"
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.zitadel.rule=Host(`${ZITADEL_EXTERNALDOMAIN}`)"
      - "traefik.http.routers.zitadel.entrypoints=web"
      - "traefik.http.services.zitadel.loadbalancer.server.port=8080"
      - "traefik.http.routers.zitadel.service=zitadel"
    healthcheck:
      test: ["CMD", "/app/zitadel", "ready"]
      interval: 10s
      timeout: 60s
      retries: 5
      start_period: 30s
    user: "0"

  # Zitadel Login UI v2
  zitadel-login:
    image: ghcr.io/zitadel/zitadel-login:latest
    restart: unless-stopped
    environment:
      ZITADEL_API_URL: http://zitadel:8080
      NEXT_PUBLIC_BASE_PATH: /ui/v2/login
    network_mode: service:zitadel
    depends_on:
      zitadel:
        condition: service_healthy

  # NestJS Backend API
  server:
    build:
      context: . # EXPLICIT: Build from repository root
      dockerfile: apps/server-nest/Dockerfile # EXPLICIT: Path to Dockerfile
      args:
        NODE_ENV: production
    ports:
      - "3002:3002"
    environment:
      # Database
      POSTGRES_HOST: db
      POSTGRES_PORT: 5432
      POSTGRES_USER: ${POSTGRES_USER:-spec}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB:-spec}

      # Zitadel
      ZITADEL_ISSUER: ${ZITADEL_ISSUER}
      ZITADEL_CLIENT_ID: ${ZITADEL_CLIENT_ID}
      ZITADEL_CLIENT_SECRET: ${ZITADEL_CLIENT_SECRET}
      ZITADEL_MAIN_ORG_ID: ${ZITADEL_MAIN_ORG_ID}

      # Server
      PORT: 3002
      NODE_ENV: production
      CORS_ORIGIN: ${CORS_ORIGIN}

      # Google AI
      GOOGLE_API_KEY: ${GOOGLE_API_KEY}
      EMBEDDING_DIMENSION: ${EMBEDDING_DIMENSION:-1536}

      # Security
      INTEGRATION_ENCRYPTION_KEY: ${INTEGRATION_ENCRYPTION_KEY}

      # Features
      DB_AUTOINIT: ${DB_AUTOINIT:-true}
      ORGS_DEMO_SEED: ${ORGS_DEMO_SEED:-false}
      CHAT_ENABLE_MCP: ${CHAT_ENABLE_MCP:-1}
    depends_on:
      db:
        condition: service_healthy
      zitadel:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3002/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # React Admin Frontend
  admin:
    build:
      context: . # EXPLICIT: Build from repository root
      dockerfile: apps/admin/Dockerfile # EXPLICIT: Path to Dockerfile
      args:
        VITE_API_URL: ${VITE_API_URL}
        VITE_ZITADEL_ISSUER: ${VITE_ZITADEL_ISSUER}
        VITE_ZITADEL_CLIENT_ID: ${VITE_ZITADEL_CLIENT_ID}
        VITE_APP_ENV: ${VITE_APP_ENV:-production}
    expose:
      - "3000"
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.admin.rule=PathPrefix(`/`)"
      - "traefik.http.routers.admin.entrypoints=web"
      - "traefik.http.services.admin.loadbalancer.server.port=3000"
      - "traefik.http.routers.admin.service=admin"
    depends_on:
      - server
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/health"]
      interval: 30s
      timeout: 10s
      retries: 3

volumes:
  postgres_data:
    driver: local
