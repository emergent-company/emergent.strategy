#!/usr/bin/env node

console.error('This script has been retired. Use "nx run workspace-cli:workspace:start" and related workspace commands instead.');
throw new Error(
    "[REMOVED] scripts/dev-manager.mjs is no longer available. Use workspace:* scripts."
);
